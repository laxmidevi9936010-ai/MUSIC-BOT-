# Telegram Railway Bot

## Railway Variables
Set these in Railway → Project → Variables:

- API_ID
- API_HASH
- BOT_TOKEN
- ASSISTANT_SESSION
- OWNER_ID

## Start Command
Railway usually detects Procfile automatically. If needed, set:

```bash
python main.py
```

## Test
Send `/start` or `/ping` to your bot.
